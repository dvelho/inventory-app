on: [push]
permissions:
  id-token: write
  contents: read #This is required for actions/checkout@v2
jobs:
  native_build:
    runs-on: ubuntu-latest
    env:
      AWS_DEFAULT_REGION: eu-west-1

    name: Build native Quarkus app
    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_TO_ASSUME }}
          role-session-name: "GitHubActions"
          aws-region: ${{ env.AWS_DEFAULT_REGION }}
      - name: Checkout the code
        uses: actions/checkout@master
      - name: Config graalvm
        uses: graalvm/setup-graalvm@v1
        with:
            version: 'latest'
            java-version: '17'
            components: 'native-image'
      - name: Set up Homebrew
        id: set-up-homebrew
        uses: Homebrew/actions/setup-homebrew@master
      - name: Maven Clean and Verify
        run: |
          mvn clean verify -Dnative -DskipTests
            - name: Maven Clean and Verify
      - name: Invoke local native app
        run: |
          sam local invoke --template target/sam.native.yaml --event payload.json
      - name: Sam build
        run: |
          sam build --template target/sam.native.yaml
      - name: Sam deploy
        run: |
          sam deploy --template-file .aws-sam/build/template.yaml --stack-name inventory-app --capabilities CAPABILITY_IAM
      - name: Upload native executable
        uses: actions/upload-artifact@v2
        with:
          name: function
          path: target/function.zip
